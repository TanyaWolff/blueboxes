 <% @volunteer.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
   <% end %>

<%= form_for(@volunteer) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f, :button => 'Create', :title =>'New volunteer'}  %>
<% end %>

<%= link_to 'Back', volunteers_path %>
